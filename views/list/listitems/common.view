#define TextStyleItem() {
  //font: $ui.font;
  font: translate($self.model.metadata.font, $self.model.metadata.font,
  "default", $ui.font);
  shadow: true;
  outline: true;
}

#define VerticalSeparator() {
  widget(backdrop, {
    height: 1;
    source: "img/separator_horizontal.png";
  });
}
