widget(loader, {
  source: select(isVoid($clone.focused.metadata.series.title) && $clone.focused.metadata.source == "themoviedb.org",
                 "video_movie.view",     // movie
                 "video_default.view");  // normal
});
