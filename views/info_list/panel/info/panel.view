widget(container_y, {
  height: $ui.height;
  width: $ui.width * 0.35;

  widget(loader, {
    time: 0.1;
    effect: blend;

    source: "items/" +
      translate($clone.focused.type, "default",
                "image", "image",
                "video", "video")	+ ".view";
  });
});
